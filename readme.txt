We’ll use:

Frontend: HTML, CSS, JavaScript (no framework, just simple static files)

Backend: Python with Flask (a lightweight web framework)

Database: SQLite (built-in with Python)

Deployment: Run locally using Flask’s development server
